@page
@model BooksApp.Pages.BooksAuthorsPages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<p>
    <a asp-page="Create">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BooksAuthors[0].Authors)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BooksAuthors[0].Books)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BooksAuthors) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Authors.AuthorId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Books.BookId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BookAuthorid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BookAuthorid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BookAuthorid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
